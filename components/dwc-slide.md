---
icon: square
---

# DWC Slide

A single slide, placed inside the **DWC Slider**'s `Slides` slot. It's a pure layout wrapper (`<li class="splide__slide">`) around whatever content you put inside it — it has **no configurable settings of its own**.

***

## Slots

| Slot      | Description                          |
| --------- | --------------------------------------- |
| `Content` (`default`) | The slide's content — image, text, anything. |
